    `:focus-visible` stroke (inline `<style>` — globals.css untouched).
- Integrated into `src/components/stages/UploadStage.tsx`:
  - Removed the flat `grid grid-cols-2 … md:grid-cols-6` Bloom button
    grid and the standalone blurb box.
  - New layout in a `glass Card`: `md:grid-cols-[auto_1fr]` — wheel on
    the left, blurb + `DataChip` + verb label on the right (stacks on
    mobile).
  - Kept heading + helper text; passed `value={bloomLevel}` /
    `onChange={setBloomLevel}`.
  - Removed unused `BLOOM_LEVELS` / `BloomLevel` imports; kept
    `BLOOM_META` (still used) and `cn` (still used in dropzone).
  - Dropzone, preview, sample-diagram affordance, CTA, run/reset logic
    all left intact.
- Verified: `bun run lint` clean (no errors). Dev server compiles and
  serves `GET / 200` after edits.

Stage Summary:
The Upload stage's Bloom level selector is now a signature interactive
radial wheel moment instead of a flat button grid. The wheel is a
reusable, fully controlled, accessible, motion-respecting component
that drops cleanly into UploadStage (and could be reused elsewhere).
No shared types, store, globals.css, layout, or three/ files were
mutated.

---
Task ID: 5
Agent: orchestrator (proactive enhancements)
Task: Implement a cohesive set of UI/animation/UX improvements across the platform

Work Log:
- **Enhanced 3D hero** (Task 5-A, subagent): particle starfield (520 points, single THREE.Points), pulsing data-flow along all 23 edges (staggered emissive spheres), pronounced highlight-node breathing, 1.5s cinematic camera dolly intro, volumetric fog for depth, live "SYSTEM ONLINE · 4 AGENTS STANDBY" status line, agent-chip pipeline connectors, HeroFallback CSS motion.
- **Bloom's taxonomy radial wheel** (Task 5-B, subagent): new `BloomWheel.tsx` — interactive SVG 6-segment annular wheel replacing the flat button grid in UploadStage. Spring-animated pointer, crossfading center, slow rotating dashed ambiance ring, full a11y (radiogroup/radio roles, keyboard), reduced-motion support. Integrated into UploadStage with wheel+blurb layout.
- **useCountUp hook** (`src/hooks/use-count-up.tsx`): rAF ease-out-cubic count-up with `CountUpText` wrapper. Integrated into ExtractionStage (entity/relationship counts), GenerationStage (question count), ResultsStage (questions/avg score/pass rate/entities). Respects reduced-motion.
- **Cursor spotlight** (`src/components/cursor-spotlight.tsx`): global soft radial glow following the pointer via CSS variables + rAF throttle. Disabled on touch/reduced-motion.
- **Scroll progress bar** (`src/components/scroll-progress.tsx`): thin emerald gradient bar at viewport top tracking page scroll (spring-smoothed).
- **Stage transitions** (`src/components/stage-transition.tsx`): AnimatePresence mode="wait" slide+fade+blur transition between stages.
- **Command palette** (`src/components/command-palette.tsx`): Cmd/Ctrl+K (or J) palette with Navigate / Difficulty / Actions groups. ⌘K hint button added to TopBar.
- **Completion burst** (`src/components/completion-burst.tsx`): one-shot particle confetti + animated check badge + radial flash when pipeline completes.
- **Results stage polish**: animated SVG verification donut (pass/flag distribution), per-card copy-to-clipboard button (with CheckCheck feedback), count-up stats, hover-lift cards.
- **page.tsx**: wired CursorSpotlight, ScrollProgress, CommandPalette, CompletionBurst, StageTransition around the AppShell.
- Lint clean. Browser-verified end-to-end: hero starfield renders, Bloom wheel interactive, stage transitions smooth, thinking console streams, completion burst fires, results donut + count-ups + copy all functional. Full pipeline produces 4 verified Q&A.

Stage Summary:
- 9 new components/hooks; 5 stage files enhanced.
- All enhancements respect prefers-reduced-motion and avoid indigo/blue.
- The platform now has: cinematic 3D hero with starfield + data-flow, radial Bloom selector, cursor spotlight, scroll progress, smooth stage transitions, Cmd+K command palette, completion celebration, animated results with donut + copy + count-ups.

---
Task ID: 6
Agent: orchestrator (reference-inspired enhancements)
Task: Implement the best ideas from the uploaded manus-ai reference (ambient orbs, results filter/sort, scroll reveals, geometric divider)

Work Log:
- Studied the uploaded manus-ai-ar2-ddcqg reference (ideas.md + 30 components). Identified 4 genuinely-better ideas not yet implemented. Re-implemented them with the project's emerald/coral palette (NOT the reference's purple/magenta, which violates the color policy).
- **Ambient floating gradient orbs** (`src/components/ambient-orbs.tsx`): 4 blurred emerald/coral/teal/amber orbs with scroll parallax (different speeds) + slow drift animation. Fixed, pointer-events-none, z-0 behind all content. Adds real atmosphere/depth. Reduced-motion renders static.
- **Results FilterBar + Sort** (`src/components/stages/ResultsFilterBar.tsx`): a genuine functional gap filled. Filter by Bloom level (multi-select chips colored by hue), sort (score desc/asc, difficulty asc/desc, verified-first), verified-only toggle, clear button, live "N shown" count. `applyFilters` helper + `FilterState` type. Integrated into ResultsStage with local state; cards wrapped in `AnimatePresence mode="popLayout"` + `layout` so they reorder/remove smoothly. Empty-state when no matches.
- **RevealOnScroll choreography** (`src/components/reveal.tsx`): `RevealOnScroll` (direction-aware fade+slide on viewport enter) + `StaggerContainer`/`StaggerItem`. Applied to StageFrame children (delayed fade-up) and the ResultsFilterBar. Reduced-motion = instant.
- **Geometric SVG section divider** (`src/components/section-divider.tsx`): placed between the 3D hero and the flat console. Diagonal sheared plane + thin emerald accent line + node-edge graph motif (emerald + coral nodes) + animated data-flow dot travelling across. Reinforces the diagram→graph concept. Reduced-motion skips the dot.
- **page.tsx**: wrapped hero/console/footer in `relative z-10` containers so the z-0 AmbientOrbs sit behind them. Mounted AmbientOrbs + SectionDivider.
- Excluded `upload/**` and `mini-services/**` from eslint (reference files were polluting lint).
- Lint clean. Browser-verified: orbs + divider render on load, full pipeline produces 4 verified Q&A, filter bar appears, verified-only toggle works, sort dropdown opens + reorders questions (verified score order 1.0,1.0,1.0,0.9), no console errors.

Stage Summary:
- 4 new components; ResultsStage gained real filter/sort functionality + animated list reordering.
- The platform now has ambient atmospheric depth (orbs), a signature diagram→graph divider, choreographed scroll reveals, and a fully functional results filter/sort system.
- All enhancements use emerald/coral/teal/amber palette (no indigo/blue/purple), respect reduced-motion.

---
Task ID: 7
Agent: orchestrator (world-class frontend overhaul)
Task: Comprehensive premium frontend upgrade — world-class 3D, film grain, custom cursor, gradient text, refined design system

Work Log:
- **Refined design system** (`globals.css` rewrite): deeper near-black palette with teal-blue warmth (oklch 0.115 0.014 245), refined glassmorphism (3 tiers: glass/glass-strong/glass-primary), `.text-gradient` utility (emerald→teal gradient clip), `.aurora-mesh` background, shimmer skeleton, premium focus rings, custom scrollbar. Film grain overlay (SVG fractalNoise, 3.5% opacity, mix-blend overlay) added to layout — the signature premium texture.
- **World-class 3D hero** (`DiagramGraphScene.tsx` rewrite): crystalline geometry — icosahedrons (highlight nodes) + octahedrons (satellites) with metallic+emissive materials + flatShading. Additive-blended glow halos behind every node. Wireframe overlay on highlight nodes. Curved quadratic-bezier edges (not straight lines) with traveling pulses sampling the curve. Volumetric particle cloud (800 points, spherical shell, per-vertex colors+sizes, additive blending, slow rotation). Cinematic 2s camera dolly (z=16→9) + handheld drift + pointer parallax. 4-light setup (ambient+hemi+directional+2 colored point lights) for rim lighting. Environment dome. Fog tuned (14-42).
- **Premium hero overlay** (`Hero3D.tsx`): gradient text on "verified question set" headline, premium glowing CTA button (gradient bg + box-shadow glow + hover scale), refined typography.
- **Custom cursor** (`custom-cursor.tsx`): two-part cursor — instant dot + spring-lagged ring that grows on hover over interactive elements. Disabled on touch/reduced-motion. The signature Linear/Vercel/Arc interaction.
- **Magnetic button** (`magnetic-button.tsx`): button that attracts toward cursor with spring physics. Reusable.
- **Gradient brand mark**: sidebar "AR2-DDCQG" uses `.text-gradient`.
- Wired CustomCursor into page.tsx alongside AmbientOrbs, CursorSpotlight, ScrollProgress, CommandPalette, CompletionBurst, SectionDivider.
- Lint clean. Browser-verified: hero renders with crystalline 3D + grain + cursor, full pipeline completes (4 verified Q&A, avg 1.00, 100% pass), no console errors.

Stage Summary:
- The platform now has: crystalline 3D geometry with additive bloom, volumetric particle cloud, film grain texture, custom two-part cursor, gradient text, premium glowing CTA, refined deep-black palette.
- All enhancements respect prefers-reduced-motion and the emerald/coral palette (no indigo/blue/purple).

---
Task ID: 8-A
Agent: full-stack-developer (landing page builder)

Task: Build the marketing landing page at `/` (hero + features + how-it-works + CTA + footer) for the multi-page product split. Overwrites the old single-page AppShell console entry — the console now lives at `/app` and auth at `/login`.

Work Log:
- Rewrote `src/app/page.tsx` (overwrite, `'use client'`, default export `LandingPage`). Structure:
  1. **Global layers** — `<AmbientOrbs />`, `<CustomCursor />`, `<ScrollProgress />` mounted at the root (these were on the old single-page app; layout only mounts ThemeProvider + Toaster + grain overlay, so they belong on the page).
  2. **Hero** — `<Hero3D onEnterConsole={() => { window.location.href = '/login' }} />`, lazy-loaded via `next/dynamic({ ssr:false })` with the same spinner fallback. The "Begin" CTA in the hero now routes to `/login` (Hero3D's internal scroll-to-console behavior is a no-op since there is no `#console` element on this page, and `onEnterConsole` fires last → navigation wins).
  3. **Features section** (`#features`) — "Six agents, *one pipeline*." heading with `.text-gradient` on "one pipeline". Eyebrow chip, centered heading block, supporting copy. A `StaggerContainer`/`StaggerItem` grid of 6 glass cards (`grid-cols-1 → sm:2 → lg:3`, `gap-5/6`):
     - Each card: faint oversized number (01–06) top-right, icon chip top-left, title, description, and a hover-reveal gradient accent line at the bottom. Hover lifts the card (`-translate-y-1`) with an emerald glow shadow.
     - Features + icons (per spec): Vision Extraction (Eye), Bloom's Taxonomy (Sparkles), Independent Answering (PenLine), Verification Loop (ShieldCheck), Quality Assurance (Lock), Live Progress (Zap).
     - Each card gets an oklch accent hue (emerald 162 / amber 95 / teal 195 / coral 25 / emerald 162 / amber 75) — NO indigo/blue/purple.
  4. **How It Works section** (`#how-it-works`) — "Six steps to *results*." heading with `.text-gradient-warm` on "results". Vertical timeline with a single gradient connecting line down the left rail (behind the dots). 6 `StepRow`s, each with a numbered/icon circle on the timeline + a glass content card containing: "Step NN" eyebrow, title, description, and a 3-bullet grid (`sm:grid-cols-3`) with tiny accent dots.
     - Steps: Upload → Extract → Generate → Answer → Verify → Export, with icons Upload, ScanLine, Wand2, PenLine, CheckCircle2, Download.
     - Each step has its own accent hue (same emerald/teal/amber/coral rotation).
     - Connecting line is an absolute `<span>` (`left-[1.4375rem] sm:left-6`, `top-2 bottom-2 w-px`, `from-foreground/0 via-foreground/12 to-foreground/0` gradient) so it visually threads through all 6 circles.
  5. **CTA section** (`#get-started`) — "Start building *today*." heading with `.text-gradient` on "today". Big `glass-strong` panel with a multi-radial gradient glow behind it (emerald top-center + coral bottom-right) and a `.grid-faint` texture overlay at 40% opacity. "Ready when you are" eyebrow chip with the pinging emerald dot. Body copy. Two CTAs: primary `<MagneticButton>` "Get started →" (same gradient/glow treatment as the hero Begin button, routes to `/login`) and a secondary "Explore the pipeline" anchor to `#features`. Trailing mono caption "No sign-up · Local sandbox · Open architecture".
  6. **Footer** (`mt-auto`) — reused the existing footer pattern: gradient brand mark "AR2-DDCQG · v1.0" + tagline on the left, agent legend (Extraction/Generation/Answering/Verification with emerald/amber/orange/rose dots) on the right. `border-t border-border/60 bg-background/80 backdrop-blur`.
- Layout wrapper: `relative flex min-h-screen flex-col bg-background` with the content column `relative z-10 flex flex-1 flex-col` so the footer (`mt-auto`) sticks to the bottom on short viewports and pushes down naturally on overflow.
- Typography: `.text-balance` on all headings, `.text-pretty` on body copy. Mono eyebrow chips with `tracking-[0.18em]` uppercase for the premium technical feel.
- Spacing rhythm: sections use `py-24 sm:py-32` (96px → 128px), inner grids use `gap-5 sm:gap-6`, heading-to-grid gap `mt-16 sm:mt-20`.
- Animations: every section heading block wrapped in `<RevealOnScroll direction="up" amount={0.4}>`; the two grids use `<StaggerContainer amount={0.1–0.15}>` + `<StaggerItem>` so cards/steps cascade in. All respect `prefers-reduced-motion` (handled inside `reveal.tsx`).
- Color policy: emerald primary, amber + coral + teal accents only. Verified no indigo/blue/purple anywhere — hues used: 162 (emerald), 195 (teal), 95/75 (amber), 25 (coral).
- Frozen files respected: did NOT touch `src/lib/*`, `src/components/three/*`, `src/app/globals.css`, or `src/app/layout.tsx`. Only overwrote `src/app/page.tsx`. Did NOT import AppShell, sidebar, console, stages, store, or socket — this page is purely presentational.
- TypeScript strict, no `any`. `LucideIcon` type used for the icon fields. `accent(hue, chroma, lightness)` helper builds oklch strings.
- No new packages installed. Used only: `next/dynamic`, `lucide-react`, and existing `@/components/*` (AmbientOrbs, CustomCursor, ScrollProgress, RevealOnScroll, StaggerContainer, StaggerItem, MagneticButton).

Verification:
- `bun run lint` — clean (0 errors, 0 warnings).
- Dev server (`dev.log`) — `GET / 200` after the rewrite, compiles cleanly (Fast Refresh did a full reload as expected for a file-level overwrite, then stable 200s).

Decisions the orchestrator / downstream agents need to know:
- **`/` is now a pure marketing landing page.** It no longer mounts the AppShell, the pipeline socket, or the Zustand store. Anyone wiring auth-protected routes should point the post-login redirect to `/app` (where the console will live) — the hero's "Begin" button and the CTA's "Get started" button both go to `/login` per the task spec.
- **`onEnterConsole` is reused as the "primary CTA clicked" hook** — its name is a bit of a misnomer now (Hero3D was built when the console was on the same page), but the export contract is frozen so we honor it. The callback navigates to `/login`.
- **Section IDs** (`#features`, `#how-it-works`, `#get-started`) are stable anchors the secondary CTA and any future nav can link to.
- **The hero's internal "Scroll to enter the console ↓" hint** still renders at the bottom of the hero. Since there is no console below the hero anymore, scrolling just reveals the features section. This is cosmetic only — flagging in case a future agent wants to tweak Hero3D's hint copy (would require touching the frozen `src/components/three/Hero3D.tsx` file, out of scope here).
- **Global layers (AmbientOrbs, CustomCursor, ScrollProgress) are mounted on this page, not in layout.** If `/login` and `/app` also want them, they'll need to mount them too — or a future refactor could lift them into `layout.tsx`. Left as page-level here to match the old single-page pattern and avoid forcing the cursor/scroll-progress onto routes that may not want them.

Stage Summary:
- `/` is now a world-class marketing landing page: cinematic 3D hero → six-agent feature grid → six-step timeline → glowing CTA → footer with agent legend.
- All five required sections built, stagger-revealed, dark-themed, emerald/coral/amber palette (no indigo/blue/purple), responsive (mobile-first, `sm:`/`lg:` breakpoints), accessible (semantic `<section>`/`<article>`/`<footer>`, `aria-labelledby` on sections, `aria-label` on the primary CTA, `aria-hidden` on decorative layers).
- Footer is `mt-auto` inside a `min-h-screen flex flex-col` wrapper — sticky-bottom compliant on short viewports, natural push on overflow.
- Lint clean, compiles, serves 200. Ready for the `/login` and `/app` routes to be built by downstream agents.

---
Task ID: 8-B
Agent: full-stack-developer (login/signup page builder)

Task: Build the premium `/login` auth page (split layout, sign-in / create-account toggle, framer-motion crossfade, emerald/coral palette).

Work Log:
- Created `src/app/login/page.tsx` (`'use client'`, default export `LoginPage`).
  - **Split layout** — root `relative flex min-h-screen bg-background`. Left half (`hidden lg:flex w-1/2`) is the visual panel; right half (`flex w-full lg:w-1/2 flex-col items-center justify-center px-6 sm:px-10 lg:px-16`) is the auth form, centered with `max-w-md`. On mobile the visual panel collapses and only the form + a small brand mark render.
  - **Ambient layers** — mounts `<AmbientOrbs />` (reused from Task 6) at the page root for the floating-orbs atmosphere across both halves. The visual panel layers `aurora-mesh` + `grid-faint` (50% opacity) + a top emerald radial glow + a bottom coral radial glow, matching the design-system "feel" called out in the task spec.
  - **Visual panel content** — top: `<BrandMark />` (Hexagon icon chip + `text-gradient` "AR2-DDCQG" mono). Middle: eyebrow chip with pinging emerald dot ("AR2-DDCQG · Multi-agent pipeline"), `text-balance` headline with `text-gradient` on "verified question set", supporting copy, and an inline animated SVG `<GraphMotif />`. Bottom: monospace status line ("System online · 4 agents standby · Local sandbox") with an emerald pip.
  - **GraphMotif SVG** — 6 nodes (INPUT/EXTRACT/GRAPH/QA labeled + 2 satellites) and 7 edges. Highlight nodes (INPUT, QA) use coral `oklch(0.74 0.19 25)`; satellites use emerald `oklch(0.84 0.17 162)`. Edges between highlight nodes tinted coral. Each edge has a framer-motion `motion.circle` traveling pulse (2.6s loop, staggered `delay = i * 0.35`) using `cx/cy/opacity` keyframes — a mini diagram→graph narrative that matches the project's signature visual. Nodes mount with a staggered scale+fade (`delay = i * 0.08`, `[0.22,1,0.36,1]` ease). All motion skipped under `prefers-reduced-motion` (static fallback).
  - **Mode toggle** — segmented control (`role="tablist"`) with two buttons (`role="tab"`, `aria-selected`). Active button gets a `motion.span` with `layoutId="auth-mode-pill"` spring-animated background (emerald→teal gradient + glow shadow) that slides between Sign in / Create account. Active text uses `var(--primary-foreground)`; inactive uses `var(--muted-foreground)`.
  - **Field set** — sign-in shows email + password; sign-up additionally shows name. The name field is wrapped in `<AnimatePresence mode="wait" initial={false}>` so it crossfades (opacity + y ±8, 220ms `[0.22,1,0.36,1]` ease) when the mode switches; email/password stay mounted (no jank).
  - **Fields** — `<Field>` helper wraps shadcn `<Label>` + `<Input>` with a left-aligned lucide icon (`User`/`Mail`/`Lock`) at `left-3 top-1/2 -translate-y-1/2`, `pl-9` input padding, `h-11 rounded-xl` sizing, `bg-foreground/[0.025]` + `focus-visible:border-primary/40` + `focus-visible:bg-foreground/[0.04]` for premium focus treatment. `aria-invalid` set only when invalid (no `aria-invalid="false"` noise). Inline hint (coral, animated in/out via AnimatePresence) shows below the field for invalid email format and password <6 chars on signup.
  - **Error alert** — `AnimatePresence` height-animated alert above the submit button. Custom styled div (not shadcn Alert) using `border-destructive/30 bg-destructive/10 text-destructive` with `<AlertCircle>` icon — keeps the premium dense look while honoring the design system. Message comes from the API JSON `{ error }` field, with sensible fallbacks.
  - **Premium submit button** — `<motion.button>` with `whileHover={{ scale: 1.02 }}` + `whileTap={{ scale: 0.98 }}` (disabled while loading). Layered: (1) absolute gradient bg (`linear-gradient(135deg, oklch(0.84 0.17 162), oklch(0.7 0.16 195))` + `boxShadow: 0 8px 32px -8px oklch(0.84 0.17 162 / 0.55), inset 0 1px 0 0 oklch(1 0 0 / 0.2)`), (2) hover sheen (`-translate-x-full → group-hover:translate-x-full`, 700ms) for the linear/arc-style sweep, (3) relative content: `<Loader2 className="animate-spin">` + "Signing in…" / "Creating account…" while loading, else "Sign in" / "Create account" + `<ArrowRight>` that nudges right on hover.
  - **Form logic** — plain `useState` (no react-hook-form needed for 3 fields). On submit: validates email regex + (signup only) name non-empty + password ≥ 6 chars client-side; POSTs JSON to `/api/auth/login` or `/api/auth/register`; on non-2xx reads `{ error }` from response and surfaces it in the alert; on 200 reads `?from=` from `window.location.search` and does `window.location.href = from || '/app'` (full reload so the session cookie is picked up by middleware). The `from` value is validated to start with `/` and NOT `//` (prevents open-redirect to external hosts). `finally { setLoading(false) }` clears the loading state. Network errors caught and surfaced as a friendly message.
  - **Legal + back link** — below the form: "By continuing you agree to our Terms and acknowledge our Privacy Policy." (muted, with slightly brighter "Terms"/"Privacy Policy"), then a centered `<Link href="/">` with `<ArrowLeft>` "Back to home".
  - **A11y** — semantic `<form>` with `noValidate` (we do our own validation), `role="tablist"`/`role="tab"`/`aria-selected` on the mode toggle, `role="alert"` on the error alert, `aria-invalid` on invalid fields, `aria-label` on the SVG motif, `aria-hidden` on decorative layers/icons. All interactive elements keyboard-reachable (native buttons/inputs/links).
  - **Color policy honored** — emerald primary (`oklch(0.84 0.17 162)`) + teal gradient stop (`oklch(0.7 0.16 195)`) + coral highlight (`oklch(0.74 0.19 25)`). NO indigo, NO blue, NO purple. All structural colors use Tailwind theme tokens (`bg-background`, `text-foreground`, `text-muted-foreground`, `border-foreground/10`, `bg-foreground/[0.025]`, `text-destructive`, `border-destructive/30`, `bg-destructive/10`).
  - **Frozen files respected** — did NOT touch `src/lib/*`, `src/app/globals.css`, `src/app/layout.tsx`, `src/middleware.ts`, or any auth API route. Only created the new `src/app/login/page.tsx` and reused `@/components/ambient-orbs`, `@/components/ui/input`, `@/components/ui/label` as-is.
  - **No new packages installed.** Used only `react`, `next/link`, `framer-motion`, `lucide-react`, and existing `@/components/*`. (react-hook-form is available but plain useState was cleaner for this 3-field form.)
  - **TypeScript strict, no `any`.** `LucideIcon` type used for the icon field. `ReadonlyArray<readonly [string, string]>` for the edges table. `Mode = 'signin' | 'signup'` discriminated union.

Verification:
- `bun run lint` — clean (0 errors, 0 warnings).
- `bunx tsc --noEmit` — 0 errors in `src/app/login/page.tsx`. (Pre-existing errors in `examples/`, `skills/`, `upload/`, `src/app/api/chat/route.ts`, `src/lib/store.ts` are outside this task's scope and were NOT touched, per the orchestrator's frozen-file rule.)
- Dev server (`dev.log`) — `GET /login 200` after the file was created (previously 404). Compiles cleanly, no Fast Refresh full-reload warnings for this file.

Decisions the orchestrator / downstream agents need to know:
- **`/login` is now a world-class split-layout auth page.** Default export `LoginPage` from `src/app/login/page.tsx`. No props, no exported sub-components — it's self-contained.
- **Redirect contract**: on successful auth, the page does `window.location.href = from || '/app'` where `from` is the validated `?from=` query param (must start with `/` and not `//`). This honors the middleware's `from` redirect (set when unauthenticated users hit `/app`) and falls back to `/app` for direct logins. Full reload (not `router.push`) is intentional so the httpOnly session cookie is sent with the next request and the middleware sees the authenticated state.
- **Middleware interplay confirmed**: the existing `src/middleware.ts` already redirects unauthenticated `/app` → `/login?from=/app` and authenticated `/login` → `/app`. The login page cooperates with this — no middleware changes were needed.
- **Visual panel is desktop-only (`hidden lg:flex`)**. On mobile the page is a single-column form with a small brand mark at the top. This matches the task spec ("On mobile: single column, form only") and keeps the form above the fold on small screens.
- **`AmbientOrbs` is mounted at the page root**, not inside the visual panel — so both halves get the floating-orb atmosphere. This is the same pattern the landing page (`/`) uses, so the two routes feel cohesive.
- **No 3D/WebGL on this page** — the visual panel uses a static SVG graph motif with framer-motion-animated pulses instead of mounting R3F. Keeps the page lightweight (no three.js bundle) and avoids WebGL context jank when navigating between `/` (which has the heavy 3D hero) and `/login`. The motif still reads as the project's signature diagram→graph narrative.
- **Form uses plain `useState`**, not `react-hook-form`. For 3 fields with simple validation this is cleaner and avoids the `mode`-switch complexity that RHF introduces (registering/unregistering the name field on mode change). If the form grows beyond ~5 fields or needs async validation, switch to RHF.
- **Submit button is `<motion.button>`, not shadcn `<Button>`** — needed for the layered gradient/glow/sheen treatment and `whileHover`/`whileTap` spring physics. The shadcn Button's variant system doesn't cleanly support a "premium gradient" variant without forking the component, so a custom motion.button was cleaner. The button still uses Tailwind theme tokens for text/disabled states.
- **Error display is a custom styled div, not shadcn `<Alert>`** — the shadcn Alert's grid layout (`has-[>svg]:grid-cols-[...]`) was overkill for a one-line error message and added unwanted padding. The custom div uses the same `border-destructive/30 bg-destructive/10 text-destructive` token treatment so it visually matches the design system. If a downstream agent wants to standardize on shadcn Alert, the swap is trivial.

Stage Summary:
- `/login` is now a premium, world-class auth page: split layout with an animated SVG graph motif on the left (desktop), centered glass-styled form on the right (with brand mark on mobile), segmented mode toggle with sliding gradient pill, crossfading name field on mode switch, inline field validation hints, height-animated error alert, premium gradient submit button with hover sheen + loading spinner, legal line, and back-to-home link.
- Honors the middleware redirect contract (`?from=` validated and used on success; full reload picks up the cookie).
- All emerald/teal/coral palette (no indigo/blue/purple), respects `prefers-reduced-motion`, dark-theme-first, responsive (mobile-first with `sm:`/`lg:` breakpoints), accessible (semantic form, ARIA roles, keyboard-reachable).
- Lint clean, tsc clean for this file, dev server serves `GET /login 200`. Ready for the `/app` route to be built by the next downstream agent.

---
Task ID: 9
Agent: orchestrator (visual + theming overhaul)
Task: Premium warm palette, dark/light toggle, display font, moving light sweep, 3D scene retint

Work Log:
- **New warm palette** (`globals.css` rewrite): replaced generic AI emerald/teal with distinctive amber-gold primary (oklch 0.82 0.15 65) + rose-copper accent. Both light and dark mode variables defined. Light mode = warm paper-white (oklch 0.985 0.008 75); dark mode = deep warm charcoal (oklch 0.12 0.012 55).
- **Premium display font**: added Bricolage Grotesque as `--font-display`, applied to all h1/h2/h3 headings. Paired against Geist body/mono. Gives big type real character.
- **Real dark/light toggle**: rewrote `theme-provider.tsx` to use `next-themes` (attribute="class", defaultTheme="dark"). Built `theme-toggle.tsx` — animated sun/moon with spring-physics sliding knob. Added to landing page (floating top-right) and app nav bar.
- **Moving light sweep**: `.light-sweep` CSS utility — a fixed conic gradient (gold + rose) that slowly rotates 360° over 40s behind all content with blur(80px). The "moving light slowly" design the user requested. Respects reduced-motion.
- **3D scene retint**: all hardcoded emerald/coral hexes in `DiagramGraphScene.tsx` → gold (#f5b942) + rose (#e85d75). Fog, dome, hemisphere light, particle colors all updated to warm palette.
- **Ambient orbs retint**: emerald→gold, coral→rose, teal→rose-magenta, amber stays amber.
- **Completion burst retint**: confetti colors → gold/rose/amber family.
- **HeroFallback retint**: all 22 SVG hex stops updated to gold/rose.
- **Bloom taxonomy ramp retint**: hue range now warm gold (85°) → rose (330°) instead of teal→green.
- **Layout**: removed hardcoded `className="dark"` from `<html>` (next-themes manages it), added `suppressHydrationWarning`, mounted light-sweep + grain in layout.
- Lint clean. Browser-verified: landing page renders 3D hero with warm gold nodes + light sweep + theme toggle; toggle switches dark↔light live; app page + login page both work; full pipeline produces 4 verified Q&A with chatbot.

Stage Summary:
- The platform now has: a distinctive warm gold/rose palette (not generic AI emerald), Bricolage Grotesque display font, a working dark/light toggle with animated knob, a slow-rotating light sweep behind all content, and the 3D hero retinted to match.
- All three pages (landing /, login /login, app /app) support both themes.